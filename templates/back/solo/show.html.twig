{% extends "front/base.html.twig" %}

{% block body %}
    <h1>Solo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ solo.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ solo.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ solo.description }}</td>
            </tr>
            <tr>
                <th>Media</th>
                <td>{{ solo.media }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('solo_index') }}">back to list</a>

    <a href="{{ path('solo_edit', {'id': solo.id}) }}">edit</a>

    {{ include('back/solo/_delete_form.html.twig') }}
{% endblock %}
