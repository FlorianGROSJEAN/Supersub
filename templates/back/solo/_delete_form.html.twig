<form method="post" action="{{ path('solo_delete', {'id': solo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ solo.id) }}">
    <button class="btn">Delete</button>
</form>
