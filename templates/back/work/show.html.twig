{% extends "front/base.html.twig" %}


{% block body %}
    <h1>Work</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ work.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ work.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ work.description }}</td>
            </tr>
            <tr>
                <th>Media</th>
                <td>{{ work.media }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_home') }}">back to list</a>

    <a href="{{ path('work_edit', {'id': work.id}) }}">edit</a>

    {{ include('back/work/_delete_form.html.twig') }}
{% endblock %}
